services:
  server:
    image: ghcr.io/planktoscope/project-docs:sha-776c201-minimal
    volumes:
      - server-data:/data
      - server-config:/config
networks:
  default:
    name: none
    external: true
volumes:
  server-data: null
  server-config: null
