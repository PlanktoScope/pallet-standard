name: check

on:
  push:
    branches:
      - main
      - edge
      - beta
      - stable
  pull_request:
  merge_group:
  workflow_dispatch:

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        forklift-version: [0.5.0, 0.4.0]
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Setup Forklift
        uses: PlanktoScope/setup-forklift@v0.1.0
        with:
          version: ${{ matrix.forklift-version }}

      - name: Run Forklift checks
        run: |
          forklift dev plt cache-repo
          forklift dev plt check
          forklift dev plt plan --parallel
          forklift dev plt plan
